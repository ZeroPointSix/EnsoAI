/** Re-exports for backward-compatible imports. */
export {
  composeSessionCanvasOutgoingMessage,
  buildSessionCanvasEnhanceMessage,
} from '@/lib/sessionCanvasComposeMessage';

export { DEFAULT_SESSION_CANVAS_PROMPT_CONFIG } from '@/lib/sessionCanvasPromptDefaults';
